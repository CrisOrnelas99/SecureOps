info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/auth/login
  body:
    type: json
    data: |
      {
        "userOrEmail": "analyst_user",
        "password": "Password123!"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
