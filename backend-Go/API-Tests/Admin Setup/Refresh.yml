info:
  name: Refresh
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/auth/refresh
  auth: inherit
  body:
    type: json
    data: |
      {
        "refreshToken": "{{refreshToken}}"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
